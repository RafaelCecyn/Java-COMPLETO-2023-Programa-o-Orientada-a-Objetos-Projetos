package entities;

public class SavingAccountPlus extends SavingsAccount {

	/*
	@Override
	public void withDraw(double amount) {
		balance -= amount + 2.0;
	} // Não pode ser sobreposto o método
	*/
	
}
